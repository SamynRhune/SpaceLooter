@using ActionCommandGame.Services.Model.Results
@model ItemResult

<tr>
    <td>@Model.Name</td>
    <td>@Model.Description</td>
    <td>@Model.Price</td>
    <td>@Model.Fuel</td>
    <td>@Model.Attack</td>
    <td>@Model.Defense</td>
    <td>@Model.ActionCooldownSeconds</td>
    <td>
        <a asp-action="ItemEdit" asp-route-id="@Model.Id" class="btn btn-secondary">Edit</a>
        <a asp-action="ItemDelete" asp-route-id="@Model.Id" class="btn btn-danger">Delete</a>
        @* <!-- Button trigger modal -->
        <button type="button" class="btn btn-danger" 
                data-bs-toggle="modal" 
                data-bs-target="#deleteModal"
                data-id="@Model.Id"
                data-role="Delete">
            Delete with modal
        </button> *@
    </td>
</tr>